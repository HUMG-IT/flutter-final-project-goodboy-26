name: flutter_final_project_goodboy_26
description: "Bài tập lớn - Ứng dụng quản lý ghi chú"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  path_provider: ^2.1.4
  localstore: ^1.4.0
  uuid: ^4.5.0
  mocktail: ^1.0.4
  flutter_slidable: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

